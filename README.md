# 🔐LSB-Stegnography
This project implements image steganography using the C programming language, where secret data is securely hidden inside BMP image files using Least Significant Bit (LSB) technique.

## 📌 Project Overview
This project demonstrates image steganography using the Least Significant Bit (LSB) technique in C. It allows users to hide secret text or files inside BMP images and retrieve them without noticeable changes to the image quality. The project emphasizes data security, bit-level manipulation, and file handling in Linux.

## 🛠️ Technologies Used
- Programming Language: C
- Operating System: Linux
- Compiler: GCC
- Tools: Makefile, Git

## ✨ Features
- Hide secret data inside BMP images
- Extract hidden data from stego images
- Uses LSB encoding technique
- Maintains original image quality
- Command-line based interface
- Error handling and validation

## 📂 Project Structure
